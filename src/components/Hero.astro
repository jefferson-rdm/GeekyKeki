<section class="hero">
  <img src="/img/teclado-hero.jpg" alt="">
  <div class="hero-text">
    <h1>Potencia tu experiencia de juego</h1>
    <p>Productos innovadores y creativos que reflejan nuestra pasion por la tecnología. ¡Hazte geek y juega con GeekyKeki!</p>
  </div>
</section>

<style lang="scss">
  .hero {
    position: relative;
    width: 100%;
    color: var(--black);
    background-color: var(--pink-soft);
    display: flex;
    justify-content: center;
    align-items: center;

    img {
      width: 100%;
      opacity: 60%;
    }
  }
  .hero-text {
    position: absolute;
    max-width: 1000px;
    display: flex;
    gap: 16px;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    
    h1 {
      font-size: 4em;
      max-width: 1000px;
      text-align: center;
      font-family: var(--font-secondary);
    }

    p {
      font-size: 1.4em;
      max-width: 800px;
      text-align: center;
    }
  }
</style>